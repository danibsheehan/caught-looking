---
description: Vitest + Testing Library tests for the React frontend
globs:
  - frontend/**/*.{test,spec}.{ts,tsx}
  - frontend/src/test/**/*
alwaysApply: false
---

# Frontend tests

- **Runner / DOM**: Vitest + jsdom; config and coverage live in **`frontend/vite.config.ts`**.
- **Setup**: **`frontend/src/test/setup.ts`** — jest-dom matchers; **`cleanup()`** after each test so the DOM does not leak between cases.
- **Conventions**: follow **`.cursor/skills/frontend-vitest-tests/SKILL.md`** for mocking `api/client`, `renderHook` + `waitFor`, user-event vs timers, and CI commands (`npm run test:run`, `npm run test:coverage`).
- **Scope**: unit / integration style tests only — no real network to production MLB; match patterns in existing `*.test.ts(x)` next to source files.
